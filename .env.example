# Copy to .env and fill in. .env is gitignored; .env.example is committed.
# Postgres is the house default (see docker-compose.yml) — adapt to your stack.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/app
