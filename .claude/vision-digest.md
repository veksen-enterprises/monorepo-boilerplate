# `<product>` — vision digest (always-on)

> **TEMPLATE — not yet authored.** This file is injected into every session by the
> `SessionStart` hook, so until it's filled it will nag you here — which is the point.
> Author `VISION.md` first, then compress it into the four items below and delete this
> banner. If this project doesn't warrant a vision, delete this file _and_ the
> `SessionStart` hook in `.claude/settings.json` rather than leaving the template live.

Short, always-in-context form of `VISION.md`. Keep it to the north star + the values;
read the full file for components, non-goals, and open questions.

**North star.** `<one sentence — what this product is and the outcome it creates>`

**Guiding principle.** `<the one rule every decision bows to; judge each change by it>`

**How the work should feel** (values, in priority order — they trade off; name the
trade-off rather than pretend it away):

- **`<Value 1>`.** `<one line>`
- **`<Value 2>`.** `<one line>`

Full detail → `VISION.md`.
