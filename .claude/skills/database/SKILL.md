---
name: database
description: Use when adding or working with a database in this repo — schema, migrations, an ORM/query builder, or a data-access layer — or when touching `db/`, schema files, drizzle/prisma config, `docker-compose.yml`, or `.env.example`. Covers local database setup and query conventions for when a database is wired in.
---

Setup and query conventions live in `database.md` beside this file — read it when wiring in or working with a database. Migration discipline lives in `.claude/rules/migrations.md`.
