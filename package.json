{
  "name": "monorepo-boilerplate",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "check-types": "turbo run check-types",
    "dev": "turbo run dev",
    "lint": "oxlint && npm run lint:design",
    "lint:fix": "oxlint --fix",
    "lint:design": "node scripts/design-lint.mjs",
    "format": "oxfmt",
    "format:check": "oxfmt --check",
    "test": "turbo run test",
    "test:coverage": "turbo run test:coverage"
  },
  "devDependencies": {
    "eslint-plugin-turbo": "^2.10.4",
    "oxfmt": "^0.58.0",
    "oxlint": "^1.73.0",
    "turbo": "^2.10.4",
    "vite": "^8.1.4"
  },
  "engines": {
    "node": "24.x",
    "npm": ">=11.6"
  },
  "packageManager": "npm@11.6.2"
}
